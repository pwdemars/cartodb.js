# Ignore these files from being linted, since they contain too many offending rules
# Generated by `find {src,test} -name "*.js" -print | awk '{print "**/" $0}' > .eslintignore`

# cartodb.js must be ignored or all files are ignored for some unknown reason
